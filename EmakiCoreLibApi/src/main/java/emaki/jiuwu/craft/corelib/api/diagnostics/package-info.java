/**
 * 诊断锚点的共享字段约定。
 *
 * <p>本包只提供**字段名常量与格式化**，不产生输出、不持有状态、不决定开关。
 * 是否记录由各模块的 debug 开关控制（模块级 + 玩家级，默认关闭），
 * 因此这些字段可以作为契约供全部模块共用，而运行期输出控制仍留在实现侧。
 *
 * <p>统一字段的目的是让实机日志可按同一套 key 关联：一次操作的线程锚点与持久化锚点
 * 共享同一 {@code op} 值，配合 {@code gen} 即可判断迟到回写是否被正确拒绝。
 */
package emaki.jiuwu.craft.corelib.api.diagnostics;
