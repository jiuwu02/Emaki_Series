import{H as e,ht as t,nt as n,tt as r}from"./chunks/framework.CXX_0q1Z.js";var i=JSON.parse(`{"title":"Scaling Curves","description":"","frontmatter":{},"headers":[],"relativePath":"en/modules/attribute/scaling-curves.md","filePath":"en/modules/attribute/scaling-curves.md","lastUpdated":0}`),a={name:`en/modules/attribute/scaling-curves.md`};function o(e,i,a,o,s,c){return t(),r(`div`,null,[...i[0]||=[n(`<h1 id="scaling-curves" tabindex="-1">Scaling Curves <a class="header-anchor" href="#scaling-curves" aria-label="Permalink to “Scaling Curves”">​</a></h1><p>Scaling curves limit late-game stat inflation. When an attribute exceeds a threshold, only the exceeding part is reduced by a curve. This keeps progression valuable while preventing equipment, gems, strengthening, and sets from breaking combat balance.</p><h2 id="recommended-use-cases" tabindex="-1">Recommended use cases <a class="header-anchor" href="#recommended-use-cases" aria-label="Permalink to “Recommended use cases”">​</a></h2><p>Apply scaling curves first to:</p><ul><li>Main damage attributes: <code>physical_attack</code>, <code>projectile_attack</code>, <code>spell_attack</code>.</li><li>Defense attributes: <code>physical_defense</code>, <code>projectile_defense</code>, <code>spell_defense</code>.</li><li>Probability attributes such as critical chance, dodge chance, and lifesteal chance.</li><li>Resource caps such as health and mana.</li><li>Penetration, damage reduction, cooldown reduction, or other attributes near hard caps.</li></ul><h2 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to “Configuration”">​</a></h2><p>Scaling curves are configured in <code>config.yml</code>. The section is empty by default, so no decay applies until you add a rule.</p><div class="language-yaml"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">scaling_curves</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">  physical_attack_cap</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">    attribute</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;physical_attack&quot;</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">    threshold</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">500</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">    curve_type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;logarithmic&quot;</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">    factor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">100</span></span></code></pre></div><p>The entry key is the rule id. When <code>attribute</code> is omitted the key itself is used as the attribute id, so the rule above can also be written as <code>physical_attack: {threshold: 500, factor: 100}</code>.</p><table tabindex="0"><thead><tr><th>Field</th><th>Description</th></tr></thead><tbody><tr><td><code>attribute</code></td><td>Attribute id affected by this rule.</td></tr><tr><td><code>threshold</code></td><td>Value where diminishing returns begin; negative values are treated as <code>0</code>.</td></tr><tr><td><code>curve_type</code></td><td><code>logarithmic</code> (default), <code>sqrt</code>, or <code>piecewise_linear</code> (alias <code>linear</code>). Unrecognized values fall back to <code>logarithmic</code> and are reported by the config precheck.</td></tr><tr><td><code>factor</code></td><td>Curve factor; exact meaning depends on curve type. <code>logarithmic</code> / <code>sqrt</code> divide by it, so values <code>&lt;= 0</code> are treated as <code>1</code>. <code>piecewise_linear</code> uses it as a retention ratio, where <code>0</code> is valid and drops the whole excess.</td></tr></tbody></table><p>Final value can be understood as:</p><div class="language-text"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>final = threshold + reduced_excess</span></span></code></pre></div><h2 id="curve-types" tabindex="-1">Curve types <a class="header-anchor" href="#curve-types" aria-label="Permalink to “Curve types”">​</a></h2><h3 id="logarithmic" tabindex="-1"><code>logarithmic</code> <a class="header-anchor" href="#logarithmic" aria-label="Permalink to “logarithmic”">​</a></h3><p>Good for main damage and defense attributes. Growth continues after the threshold but slows down smoothly.</p><h3 id="sqrt" tabindex="-1"><code>sqrt</code> <a class="header-anchor" href="#sqrt" aria-label="Permalink to “sqrt”">​</a></h3><p>Also has a decreasing slope. Suitable for defense, mitigation, or resource caps.</p><div class="caution custom-block"><p class="custom-block-title custom-block-title-default">CAUTION</p><p>Two caveats:</p><ol><li>At the same <code>factor</code>, sqrt is actually <strong>weaker</strong> than logarithmic (with <code>factor: 100</code> and an excess of 500, logarithmic yields 179 while sqrt yields 224). For stricter control, use logarithmic or lower <code>factor</code> substantially.</li><li>It is implemented as <code>sqrt(factor * excess)</code>, so when the <strong>excess is smaller than <code>factor</code></strong> the result exceeds the excess, amplifying instead of reducing. With <code>factor: 80</code>, a raw value of 440 (excess 40) resolves to <code>400 + 56.6 = 456.6</code>. Keep <code>factor</code> below the smallest excess you care about to stay out of that range.</li></ol></div><h3 id="piecewise-linear" tabindex="-1"><code>piecewise_linear</code> <a class="header-anchor" href="#piecewise-linear" aria-label="Permalink to “piecewise_linear”">​</a></h3><p>Keeps a fixed ratio of the excess part. Useful for probability attributes or rules that should be easy to reason about.</p><div class="note custom-block"><p class="custom-block-title custom-block-title-default">NOTE</p><p>Despite the name, this is a single <code>excess * factor</code> discount and <strong>does not support multiple segments</strong>. The name is kept for compatibility with existing configs.</p></div><div class="language-yaml"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">scaling_curves</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">  crit_rate_cap</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">    attribute</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;physical_crit_rate&quot;</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">    threshold</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">80</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">    curve_type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;piecewise_linear&quot;</span></span>
<span class="line"><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">    factor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.3</span></span></code></pre></div><p>If the raw value is 100, the final value is about <code>80 + (20 * 0.3) = 86</code>.</p><div class="note custom-block"><p class="custom-block-title">Relationship with attribute value limits</p><p>Scaling curves run <strong>first</strong>; the <code>min_value</code> / <code>max_value</code> / <code>allow_negative</code> limits from the attribute definition run <strong>after</strong>. The full order is: aggregate all sources → scaling curves → derived attributes → value limits.</p><p>A curve result is therefore still bound by the attribute&#39;s own range. The <code>86</code> above sits inside <code>physical_crit_rate</code>&#39;s <code>[0, 100]</code> range and is unaffected, but if a loose curve still leaves the value above <code>max_value</code>, the final value is pushed back to <code>max_value</code>. The two layers complement each other: curves keep growth smooth, ranges enforce the hard cap.</p></div><h2 id="tuning-notes" tabindex="-1">Tuning notes <a class="header-anchor" href="#tuning-notes" aria-label="Permalink to “Tuning notes”">​</a></h2><ul><li>Set thresholds near reasonable endgame values, not beginner values.</li><li>Avoid multiple curve rules for the same attribute unless you have a clear reason.</li><li>Test low, mid, endgame, and extreme-stacked characters after changing curves.</li><li>If growth feels too weak, increase <code>threshold</code> or <code>factor</code>; if too strong, lower them or use a stronger curve.</li></ul>`,26)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};