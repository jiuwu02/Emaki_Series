import{H as e,ht as t,nt as n,tt as r}from"./chunks/framework.CXX_0q1Z.js";var i=JSON.parse(`{"title":"Events","description":"","frontmatter":{},"headers":[],"relativePath":"en/modules/item/events.md","filePath":"en/modules/item/events.md","lastUpdated":0}`),a={name:`en/modules/item/events.md`};function o(e,i,a,o,s,c){return t(),r(`div`,null,[...i[0]||=[n(`<h1 id="events" tabindex="-1">Events <a class="header-anchor" href="#events" aria-label="Permalink to “Events”">​</a></h1><table tabindex="0"><thead><tr><th>Event</th><th>Timing/thread</th><th>Cancellable</th><th>Mutable field</th><th>Cancellation/effect</th></tr></thead><tbody><tr><td><code>EmakiItemCreateEvent</code></td><td>After a stack is built, before it leaves the factory; global-region owner</td><td>Yes</td><td><code>result</code></td><td>Cancellation discards the stack and public <code>create</code> returns <code>CANCELLED</code>; listeners may replace the returned stack. Current runtime creation supplies no player, so <code>getPlayer()</code> is null.</td></tr><tr><td><code>ItemRepairEvent</code></td><td>After validation, before charge and durability commit; player owner</td><td>Yes</td><td><code>restoreAmount</code></td><td>Cancellation prevents charge and repair. Amount <code>&lt;= 0</code> suppresses repair.</td></tr><tr><td><code>ItemSetBonusChangeEvent</code></td><td>After the active equipped-piece count changes; player owner</td><td>No</td><td>None</td><td>Informational fields include old/new active count, total pieces, active thresholds, and trigger.</td></tr><tr><td><code>ItemStateChangeEvent</code></td><td>After a committed item-state mutation; owner thread that performed the mutation</td><td>No</td><td>None</td><td>Post-notification that cannot be rolled back. Exposes <code>getItem()</code> (a clone), <code>getMutation()</code>, <code>getHolder()</code> (null when no holder was known), and the forwarded <code>getKey()</code>, <code>getOldValue()</code>, <code>getNewValue()</code>, <code>getDelta()</code>.</td></tr><tr><td><code>ItemStateThresholdEvent</code></td><td>After a committed numeric mutation crossed a configured threshold; owner thread that performed the mutation</td><td>No</td><td>None</td><td>Post-notification that cannot be rolled back. Exposes <code>getItem()</code>, <code>getHolder()</code>, <code>getKey()</code>, <code>getOldValue()</code>, <code>getNewValue()</code>, <code>getThreshold()</code>, <code>getThresholdId()</code>, <code>getDirection()</code> (<code>UP</code>/<code>DOWN</code>), <code>isOnce()</code>, and <code>isRearmed()</code>.</td></tr></tbody></table><p><code>ItemOperations.create</code> requires global-region ownership so its synchronous creation event is fired before return. Repair events cover the runtime repair services; migration and refresh operations do not invent migration/refresh event classes. There is no <code>ItemUpdateEvent</code> or <code>EquipmentSkillTriggerEvent</code> in the source. The event package is <code>emaki.jiuwu.craft.item.api.event</code>.</p><p><code>ItemStateThresholdEvent</code> only covers numeric keys (<code>INTEGER</code>, <code>LONG</code>, <code>DOUBLE</code>) that have at least one configured threshold; one event is dispatched per crossed threshold, ascending for upward crossings and descending for downward ones. It is not fired for rejected or unchanged mutations, reserved <code>meta.</code> metadata keys, <code>BOOLEAN</code> and <code>STRING</code> keys, numeric keys without configured thresholds, values that stay on the same side of every threshold, a <code>once</code> threshold still latched from an earlier crossing, or state restored verbatim by rebuild preservation.</p>`,4)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};