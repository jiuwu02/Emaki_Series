import{_ as t,o as a,c as o,ag as s}from"./chunks/framework.DaCm-iTo.js";const g=JSON.parse('{"title":"Capacity","description":"","frontmatter":{},"headers":[],"relativePath":"en/modules/storage/capacity.md","filePath":"en/modules/storage/capacity.md","lastUpdated":null}'),i={name:"en/modules/storage/capacity.md"};function d(c,e,n,l,r,h){return a(),o("div",null,[...e[0]||(e[0]=[s(`<h1 id="capacity" tabindex="-1">Capacity <a class="header-anchor" href="#capacity" aria-label="Permalink to &quot;Capacity&quot;">​</a></h1><p>This page covers EmakiStorage&#39;s capacity domain model: the <code>capacity</code> section of <code>config.yml</code>, the four capacity sources, the three-level <code>stackLimit</code>, and what happens to occupancy when capacity shrinks.</p><h2 id="capacity-1" tabindex="-1">capacity <a class="header-anchor" href="#capacity-1" aria-label="Permalink to &quot;capacity&quot;">​</a></h2><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">capacity</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  base_slots</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">45</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  max_slots</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1000</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  warn_entry_count</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">5000</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  default_stack_limit</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">100</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>base_slots</code></td><td>int</td><td><code>45</code></td><td>Default open slot count (45 = 5 × 9, matching the <code>gui.storage_rows</code> default).</td></tr><tr><td><code>max_slots</code></td><td>int</td><td><code>1000</code></td><td>Hard ceiling on total slots. <code>0</code> means unlimited.</td></tr><tr><td><code>warn_entry_count</code></td><td>int</td><td><code>5000</code></td><td>Logs a console warning when the entry count exceeds this value. <code>0</code> disables the warning.</td></tr><tr><td><code>default_stack_limit</code></td><td>long</td><td><code>100</code></td><td>Config-level per-slot ceiling. At this level <code>0</code> means unlimited.</td></tr></tbody></table><h3 id="four-capacity-sources" tabindex="-1">Four Capacity Sources <a class="header-anchor" href="#four-capacity-sources" aria-label="Permalink to &quot;Four Capacity Sources&quot;">​</a></h3><p><code>effectiveSlots</code> is the sum of four independently persisted sources, then clamped:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>effective = clamp(base_slots + permission tier + grantedSlots + purchasedSlots, 0, max_slots)</span></span></code></pre></div><p>The upper clamp is skipped when <code>max_slots</code> is <code>0</code>. Because the four sources are stored separately, lowering <code>base_slots</code> never consumes slots a player was granted or purchased.</p><table tabindex="0"><thead><tr><th>Source</th><th>Comes from</th><th>Can be negative</th></tr></thead><tbody><tr><td><code>baseSlots</code></td><td><code>capacity.base_slots</code></td><td>No (negatives treated as 0)</td></tr><tr><td><code>permissionSlots</code></td><td>Highest <code>emakistorage.slots.&lt;n&gt;</code> tier</td><td>No</td></tr><tr><td><code>grantedSlots</code></td><td><code>/estorage slot grant</code>, API <code>grantSlots</code>, action <code>storage-grant-slot</code></td><td>Yes</td></tr><tr><td><code>purchasedSlots</code></td><td>In-GUI paid expansion, action <code>storage-unlock-slot</code></td><td>No</td></tr></tbody></table><p>Page count is derived from slot count and cannot be configured in reverse:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>totalPages     = max(1, ceil(effectiveSlots / slots per page))</span></span>
<span class="line"><span>reachablePages = max(1, ceil(usedSlots / slots per page))</span></span></code></pre></div><p>Page 1 is always reachable (an empty warehouse must still open and accept its first item); a page holding no entry at all cannot be paged into.</p><p>Confirm you have enough memory before setting <code>max_slots</code> to <code>0</code> (unlimited). The figure given in the config comment is roughly 100–200MB of heap for 100,000 entries on one player; that is an estimate and has not been measured.</p><h2 id="three-level-stacklimit" tabindex="-1">Three-Level stackLimit <a class="header-anchor" href="#three-level-stacklimit" aria-label="Permalink to &quot;Three-Level stackLimit&quot;">​</a></h2><p>The per-slot ceiling resolves most-specific-first across three levels:</p><table tabindex="0"><thead><tr><th>Level</th><th>Stored in</th><th>Set through</th><th>Meaning of <code>0</code></th></tr></thead><tbody><tr><td>Entry</td><td>The matching entry in <code>meta.yml</code></td><td><code>/estorage stacklimit slot</code>, API <code>setSlotStackLimit</code>, action <code>storage-set-stacklimit</code> with <code>slot</code></td><td>Inherit the player level</td></tr><tr><td>Player</td><td><code>defaultStackLimit</code> in <code>meta.yml</code></td><td><code>/estorage stacklimit player</code>, API <code>setStackLimit</code>, action <code>storage-set-stacklimit</code> without <code>slot</code></td><td>Inherit the config level</td></tr><tr><td>Config</td><td><code>capacity.default_stack_limit</code> in <code>config.yml</code></td><td>Config file</td><td><strong>Unlimited</strong> (equivalent to <code>Long.MAX_VALUE</code>)</td></tr></tbody></table><p>The same <code>0</code> means different things per level: at entry and player level it means &quot;inherit the next level&quot;, at config level it means &quot;no limit&quot;. Resolution order is entry level <code>&gt; 0</code>, then player level <code>&gt; 0</code>, then the config level; a config level <code>&lt;= 0</code> yields <code>Long.MAX_VALUE</code>.</p><p>The <code>emakistorage.stacklimit.&lt;n&gt;</code> permission tier can also supply a player-level ceiling, again taking the highest value and not supporting wildcards.</p><h2 id="shrinking-and-overflow" tabindex="-1">Shrinking and Overflow <a class="header-anchor" href="#shrinking-and-overflow" aria-label="Permalink to &quot;Shrinking and Overflow&quot;">​</a></h2><p>When a lowered capacity leaves occupancy beyond the boundary, the outcome is decided by <code>unlock.overflow_policy</code>. The key itself is documented in the <code>unlock</code> section of <a href="./configuration.html">Configuration</a>.</p><h3 id="the-four-overflow-policy-values" tabindex="-1">The Four overflow_policy Values <a class="header-anchor" href="#the-four-overflow-policy-values" aria-label="Permalink to &quot;The Four overflow_policy Values&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Value</th><th>Behaviour</th></tr></thead><tbody><tr><td><code>lock_readonly</code></td><td>Entries beyond the capacity boundary become read-only: withdrawable, not depositable, released as soon as they are emptied. Recommended.</td></tr><tr><td><code>compact</code></td><td>Overflowing entries roll forward into free slots. Entries are already stored in a gap-free compact list, so &quot;roll forward&quot; is the list&#39;s natural state; whatever still does not fit degrades to <code>lock_readonly</code>.</td></tr><tr><td><code>return_inventory</code></td><td>Tries to hand overflowing entries back to the player&#39;s inventory; whatever does not fit degrades to <code>lock_readonly</code>. Nothing is returned while the player is offline, entries are simply locked.</td></tr><tr><td><code>reject_change</code></td><td>Refuses the shrink outright.</td></tr></tbody></table><p>All four policies are zero-loss. There is deliberately <strong>no</strong> <code>drop</code> or <code>delete</code>: irreversible data loss must never be triggered implicitly by adjusting a config value.</p><p>Overflow state is not persisted. It is derived from capacity versus occupancy and recomputed whenever capacity changes — login, reload, permission change, command grant — so a stored flag can never drift out of sync with the facts.</p><h2 id="related-pages" tabindex="-1">Related Pages <a class="header-anchor" href="#related-pages" aria-label="Permalink to &quot;Related Pages&quot;">​</a></h2><ul><li><a href="./configuration.html">Configuration</a>: the remaining <code>config.yml</code> sections such as <code>unlock</code>, <code>behavior</code> and <code>persistence</code>.</li><li><a href="./gui.html">GUI and Display</a>: how <code>gui.storage_rows</code> decides the slots per page.</li><li><a href="./unlock-costs.html">Unlock Costs</a>: pricing and the purchase flow behind <code>purchasedSlots</code>.</li><li><a href="./commands.html">Commands and Permissions</a>: <code>/estorage slot</code>, <code>/estorage stacklimit</code> and the permission tiers.</li><li><a href="./api.html">API and Integration</a>: how <code>StorageCapacity</code> fields map onto configuration keys.</li></ul>`,27)])])}const u=t(i,[["render",d]]);export{g as __pageData,u as default};
