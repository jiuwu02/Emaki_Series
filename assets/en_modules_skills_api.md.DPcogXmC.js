import{H as e,ht as t,nt as n,tt as r}from"./chunks/framework.CXX_0q1Z.js";var i=JSON.parse(`{"title":"Public API","description":"","frontmatter":{},"headers":[],"relativePath":"en/modules/skills/api.md","filePath":"en/modules/skills/api.md","lastUpdated":0}`),a={name:`en/modules/skills/api.md`};function o(e,i,a,o,s,c){return t(),r(`div`,null,[...i[0]||=[n(`<h1 id="public-api" tabindex="-1">Public API <a class="header-anchor" href="#public-api" aria-label="Permalink to “Public API”">​</a></h1><h2 id="compile-time-dependency" tabindex="-1">Compile-time dependency <a class="header-anchor" href="#compile-time-dependency" aria-label="Permalink to “Compile-time dependency”">​</a></h2><div class="language-xml"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">xml</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">repositories</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;&lt;</span><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">repository</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;&lt;</span><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;jiuwu-releases&lt;/</span><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;&lt;</span><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">url</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;https://repo.crypticlib.com/repository/maven-public/&lt;/</span><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">url</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;&lt;/</span><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">repository</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;&lt;/</span><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">repositories</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">dependency</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;&lt;</span><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">groupId</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;emaki.jiuwu.craft&lt;/</span><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">groupId</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;&lt;</span><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">artifactId</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;emaki-skills-api&lt;/</span><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">artifactId</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;&lt;</span><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">version</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;2.8.1&lt;/</span><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">version</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;&lt;</span><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">scope</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;provided&lt;/</span><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">scope</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;&lt;/</span><span style="--shiki-light:#11782a;--shiki-dark:#85E89D;">dependency</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span></code></pre></div><div class="language-kotlin"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">kotlin</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">repositories</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">maven</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;https://repo.crypticlib.com/repository/maven-public/&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) }</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">dependencies</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">compileOnly</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;emaki.jiuwu.craft:emaki-skills-api:2.8.1&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) }</span></span></code></pre></div><div class="danger custom-block"><p class="custom-block-title">Compile only — never install, shade, or relocate this jar</p><p>The runtime already embeds this un-relocated API. A duplicate copy prevents Bukkit listeners and bridge types from matching, and the PDC codec being independently callable does not change that.</p></div><h2 id="facade-and-layers" tabindex="-1">Facade and layers <a class="header-anchor" href="#facade-and-layers" aria-label="Permalink to “Facade and layers”">​</a></h2><p>Test <code>EmakiSkillsApi.status().usable()</code>. <code>catalog()</code>, <code>operations()</code>, and <code>extensions()</code> are never null — they degrade to a stable unavailable implementation while the bridge is absent. <code>install</code>, <code>uninstall</code>, and <code>Bridge</code> are internal runtime contracts. Accessors are cheap and the bridge is replaced across a reload, so resolve the layer at the point of use rather than caching it in a field.</p><h3 id="catalog" tabindex="-1">catalog <a class="header-anchor" href="#catalog" aria-label="Permalink to “catalog”">​</a></h3><p><code>catalog()</code> provides skill definitions, player skill views, levels, learned state, cast mode, and max-level checks:</p><table tabindex="0"><thead><tr><th>Method</th><th>Thread</th><th>Description</th></tr></thead><tbody><tr><td><code>skills()</code></td><td>any</td><td>Snapshot of every loaded definition (including disabled ones), sorted by canonical id.</td></tr><tr><td><code>skill(String skillId)</code></td><td>any</td><td>One definition; a blank or unknown id yields an empty <code>Optional</code>.</td></tr><tr><td><code>playerSkills(Player)</code></td><td>owner</td><td>Slot bindings and cast-mode snapshot; <code>NOT_FOUND</code> while the profile is not loaded.</td></tr><tr><td><code>level(Player, String)</code></td><td>owner</td><td>Stored level, clamped into <code>[1, maxLevel]</code>.</td></tr><tr><td><code>upgradePreview(Player, String)</code></td><td>owner</td><td>Quotes the next upgrade: target level, success rate, currency and material costs. A quote only — nothing is charged or locked.</td></tr><tr><td><code>hasLearned(Player, String)</code></td><td>owner</td><td>Tests only the manual-unlock source; it does not mean the player can use the skill.</td></tr><tr><td><code>castModeEnabled(Player)</code></td><td>owner</td><td>Reads the cast-mode flag.</td></tr><tr><td><code>atMaxLevel(Player, String)</code></td><td>owner</td><td>Whether the skill is at its maximum. A skill without upgrades enabled has a maximum of <code>1</code>.</td></tr></tbody></table><p>Calling a per-player query off the owner thread returns <code>WRONG_THREAD</code>.</p><h2 id="operations" tabindex="-1">Operations <a class="header-anchor" href="#operations" aria-label="Permalink to “Operations”">​</a></h2><p>The asynchronous cast methods are <code>cast(Player,String)</code> and <code>castByTrigger(Player,String)</code>, both returning <code>CompletableFuture&lt;EmakiResult&lt;SkillCastOutcome&gt;&gt;</code>. They may be submitted from any thread and the runtime hops to the player&#39;s owner thread for the Bukkit, pre/post event, resource, and cooldown phases; the completion thread is not guaranteed, so a continuation that touches the player must hop back itself.</p><p><code>learn</code>, <code>forget</code>, <code>forgetAll</code>, <code>equip</code>, <code>unequip</code>, <code>bindTrigger</code>, <code>upgrade</code>, <code>setLevel</code>, <code>addLevel</code>, and <code>setCastMode</code> are synchronous owner-thread operations. A failed upgrade roll is a successful process, represented by <code>SkillUpgradeOutcome.successfulRoll()==false</code> rather than an outer Failure. <code>setLevel</code> below 1 returns <code>INVALID_INPUT</code>, and values above the maximum are clamped rather than rejected.</p><h2 id="extensions" tabindex="-1">Extensions <a class="header-anchor" href="#extensions" aria-label="Permalink to “Extensions”">​</a></h2><p><code>extensions()</code> only exposes external skill-source registration:</p><ul><li><code>registerSkillSource(Plugin owner, SkillSourceProvider provider)</code> returns a <code>SkillSourceRegistration</code>. Registering again under the same owner and provider id supersedes the previous entry, closing a superseded handle does not remove its replacement, and every registration owned by a plugin is dropped when that plugin is disabled. A null or already-disabled <code>owner</code>, or a blank <code>id()</code>, yields an inactive no-op handle.</li><li><code>SkillSourceProvider.id()</code> is unique within the owning plugin, <code>priority()</code> orders evaluation (lower first, default <code>100</code>), and <code>collect(Player)</code> is called synchronously on the player&#39;s owner thread, so it must be fast and must not touch state owned by another entity or region.</li></ul><div class="warning custom-block"><p class="custom-block-title">The script action registry is gone</p><p>Skill scripts are CoreLib pipelines now, so third parties can no longer register script action stages with EmakiSkills. To add a stage, register it with CoreLib&#39;s stage registry (<code>EmakiCoreLibApi.registerActionStage</code>); the stage then becomes available to every module at once. The twelve stages EmakiSkills registers itself are in <a href="./corelib-actions.html">CoreLib Actions</a>.</p></div><p><code>EquipmentSkillPdcCodec</code> offers low-level payload read/write/copy/clear; live equipment access still requires the owner thread.</p><h2 id="result-semantics" tabindex="-1">Result semantics <a class="header-anchor" href="#result-semantics" aria-label="Permalink to “Result semantics”">​</a></h2><p>No-payload success uses <code>EmakiResult&lt;Unit&gt;</code>; optional payloads use <code>optionalValue()</code>. The result factories are <code>success</code>, <code>partial</code>, and <code>failure</code>. <code>FailureKind</code> is exactly <code>UNAVAILABLE</code>, <code>NOT_FOUND</code>, <code>INVALID_INPUT</code>, <code>REJECTED</code>, <code>CANCELLED</code>, <code>TARGET_OFFLINE</code>, <code>WRONG_THREAD</code>, <code>INTERNAL_ERROR</code>. Classify availability through <code>status()</code> or <code>FailureKind</code>, never by catching <code>NullPointerException</code>.</p>`,21)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};