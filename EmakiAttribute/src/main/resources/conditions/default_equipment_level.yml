enabled: false

source_id: gem_equipment_level

condition_type: all_of

invalid_as_failure: true

conditions:
  - type: lore_regex
    pattern: "装备等级需求: Lv\\.(\\d+)"
    condition: "$1 <= %player_level%"
