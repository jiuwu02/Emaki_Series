id: "brewing_sources"
enabled: true
sources:
  brew_complete:
    enabled: true
    type: "brewing"
    trigger: "brew_complete"
    attribution:
      expire_ticks: 1200
    anti_abuse:
      exp_cooldown_ticks: 20
    rules:
      - potion_types:
          - "AWKWARD"
          - "SPEED"
          - "STRENGTH"
        exp_formula: "15"
