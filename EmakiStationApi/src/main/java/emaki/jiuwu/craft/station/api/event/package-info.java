/**
 * Public Bukkit events fired by EmakiStation.
 *
 * <p>Each event's own Javadoc states its stage, whether it is cancellable, which thread it fires on,
 * what it covers, and the cases in which it deliberately does not fire. None of these events is an
 * audit log.
 */
package emaki.jiuwu.craft.station.api.event;
